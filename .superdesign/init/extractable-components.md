# Extractable Components

No layout or reusable visual component is eligible for extraction yet. The rebuild intentionally has no header, footer, product card, dialog, drawer, input, carousel, or page shell before the Home design is approved.

After approval and Angular implementation, rerun repository initialization and catalogue approved Header, Footer, AppShell, ProductCard, SearchField, CartDrawer and related shared components. Do not extract the temporary foundation placeholder.
